<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.health.READ_EXERCISE" />
    <uses-permission android:name="android.permission.health.READ_WEIGHT" />
    <uses-permission android:name="android.permission.health.READ_BODY_FAT" />
    <uses-permission android:name="android.permission.health.READ_STEPS" />
    <uses-permission android:name="android.permission.health.READ_TOTAL_CALORIES_BURNED" />
    <uses-permission android:name="android.permission.health.READ_HEART_RATE" />

    <!-- Android 11+(API 30) package visibility: HC 미설치 안내 카드의 Play Store(market://) 딥링크가
         resolve 되도록 스토어 패키지를 명시. 미선언 시 ActivityNotFoundException → web fallback 으로만 동작. -->
    <queries>
        <package android:name="com.android.vending" />
    </queries>

    <application
        android:name=".EundunHealthApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EundunHealth"
        tools:targetApi="37">

        <provider
            android:name="io.sentry.android.core.SentryInitProvider"
            android:authorities="${applicationId}.SentryInitProvider"
            tools:node="remove" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.EundunHealth">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- App Links: Supabase Confirm Email 메일 링크가 앱으로 직접 전달 -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="${appLinksHost}"
                    android:pathPrefix="/auth/confirm" />
            </intent-filter>
        </activity>

        <!-- Health Connect 권한 rationale(개인정보 처리방침) 화면.
             Android 14+(API 34+) 통합 HC 는 grant 화면을 띄우기 전 이 intent 의 resolve 가능
             여부를 검사하며, 없으면 grant 화면을 거부하고 즉시 finish 한다 → "연동 버튼 무반응"
             (INC 2026-06-10, Android 15 실측). minSdk=26 이므로 14+/≤13 두 경로 모두 선언.
             회귀 가드: app/src/test/.../ManifestHealthConnectRationaleTest.kt -->
        <activity
            android:name=".PermissionsRationaleActivity"
            android:exported="true">
            <!-- Android 13 이하: Health Connect APK 가 사용하는 레거시 rationale 액션 -->
            <intent-filter>
                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
            </intent-filter>
        </activity>

        <!-- Android 14+(API 34+): 통합 Health Connect 가 요구하는 rationale 액션 + 카테고리.
             START_VIEW_PERMISSION_USAGE 권한으로 시스템(HC)만 호출 가능하도록 보호. -->
        <activity-alias
            android:name="ViewPermissionUsageActivity"
            android:exported="true"
            android:targetActivity=".PermissionsRationaleActivity"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
            </intent-filter>
        </activity-alias>
    </application>
</manifest>
