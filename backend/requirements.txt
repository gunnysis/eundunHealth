# 0.136.3 is a compromised release (MAL-2026-4750, reported 2026-05-23 by Amazon Inspector):
# pyproject.toml injects an undocumented "fastar>=0.9.0" typosquat dependency.
# `pip install "fastapi[standard]"` pulls in fastar as a side effect.
# Downgraded to safe 0.136.1 (2026-04-23). Re-evaluate when a clean 0.136.4+ is released.
# Note: 0.136.2 is 404 on PyPI (presumed yanked).
fastapi==0.136.1
# starlette: transitive dep of fastapi, explicitly pinned for security patch
# (fastapi's default resolves to 0.48 which has GHSA-7f5h-v6xp-fcq8)
starlette==0.49.1
uvicorn[standard]==0.48.0
sqlalchemy[asyncio]==2.0.50
asyncpg==0.31.0
alembic==1.18.4
pydantic-settings==2.14.1
PyJWT[crypto]==2.13.0
httpx==0.28.1
sentry-sdk[fastapi]==2.60.0
