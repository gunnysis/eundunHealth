# v0.1 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Backend를 Ktor(Kotlin)에서 FastAPI(Python)로 전환하고, Android 에러 핸들링 통일, CI/CD, 비밀번호 재설정, 회원 탈퇴, UX 개선을 구현한다.

**Architecture:** Backend는 FastAPI + SQLAlchemy async + Repository/Service 패턴. Android는 기존 Clean Architecture 유지하면서 AppError 통일 패턴 적용. CI/CD는 GitHub Actions.

**Tech Stack:** Python 3.12, FastAPI, SQLAlchemy 2.0 async, asyncpg, PyJWT, Alembic, pytest / Kotlin, Jetpack Compose, Hilt, Retrofit, Supabase

**설계서 참조:** `docs/plans/2026-05-24-implementation-spec.md` — 모든 코드는 이 설계서의 섹션 B/D/C/A/E/G/F/H/O/P/Q/R에서 가져온다.

---

## Phase 1: Backend FastAPI 프로젝트 초기화 (설계서 B)

### Task 1: Python 프로젝트 스캐폴딩

**Files:**
- Create: `backend-fastapi/requirements.txt`
- Create: `backend-fastapi/requirements-dev.txt`
- Create: `backend-fastapi/pyproject.toml`
- Create: `backend-fastapi/.env.example`
- Create: `backend-fastapi/app/__init__.py`

**Step 1: 프로젝트 디렉토리 및 requirements 생성**

`backend-fastapi/` 디렉토리를 새로 생성한다 (기존 `backend/`은 Ktor 코드 보존).
설계서 B.6의 `requirements.txt`와 `requirements-dev.txt` 내용을 그대로 작성.

**Step 2: pyproject.toml 생성**

설계서 K 섹션의 ruff + mypy 설정을 포함:
```toml
[tool.ruff]
line-length = 120
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP"]

[tool.mypy]
python_version = "3.12"
strict = true
plugins = ["pydantic.mypy"]
```

**Step 3: .env.example 생성**

설계서 B.4의 `.env.example` 내용을 그대로 작성.

**Step 4: venv 생성 및 의존성 설치 확인**

Run: `cd backend-fastapi && python -m venv .venv && .venv/Scripts/pip install -r requirements-dev.txt`
Expected: 모든 패키지 설치 성공

**Step 5: Commit**

```bash
git add backend-fastapi/
git commit -m "feat(backend): FastAPI 프로젝트 스캐폴딩"
```

---

### Task 2: 핵심 인프라 파일 (config, database, exceptions)

**Files:**
- Create: `backend-fastapi/app/config.py`
- Create: `backend-fastapi/app/database.py`
- Create: `backend-fastapi/app/exceptions.py`

**Step 1: config.py 작성**

설계서 B.3 `app/config.py` 코드를 그대로 작성. `Settings` 클래스 + `get_settings()` 함수.

**Step 2: database.py 작성**

설계서 B.3 `app/database.py` 코드를 그대로 작성. `Base` + `get_db()` UoW 패턴.

**Step 3: exceptions.py 작성**

설계서 B.3 `app/exceptions.py` 코드를 그대로 작성. `AppException`, `NotFoundException`, `ConflictException`, `BadRequestException`.

**Step 4: import 확인**

Run: `cd backend-fastapi && python -c "from app.config import get_settings; from app.database import Base; from app.exceptions import AppException; print('OK')"`
Expected: `OK`

**Step 5: Commit**

```bash
git add backend-fastapi/app/
git commit -m "feat(backend): config, database, exceptions 기반 모듈"
```

---

### Task 3: SQLAlchemy 모델

**Files:**
- Create: `backend-fastapi/app/models/__init__.py`
- Create: `backend-fastapi/app/models/user_profile.py`
- Create: `backend-fastapi/app/models/weekly_plan.py`
- Create: `backend-fastapi/app/models/badge.py`

**Step 1: 3개 모델 파일 작성**

설계서 B.3의 `user_profile.py`, `weekly_plan.py`, `badge.py` 코드를 그대로 작성.

**Step 2: models/__init__.py 작성**

설계서 B.3의 `models/__init__.py` 코드를 그대로 작성 — Base + 3개 모델 re-export.

**Step 3: import 확인**

Run: `cd backend-fastapi && python -c "from app.models import Base, UserProfile, WeeklyPlan, Badge; print(Base.metadata.tables.keys())"`
Expected: `dict_keys(['user_profiles', 'weekly_plans', 'badges'])`

**Step 4: Commit**

```bash
git add backend-fastapi/app/models/
git commit -m "feat(backend): SQLAlchemy 모델 (UserProfile, WeeklyPlan, Badge)"
```

---

### Task 4: Pydantic 스키마

**Files:**
- Create: `backend-fastapi/app/schemas/__init__.py`
- Create: `backend-fastapi/app/schemas/base.py`
- Create: `backend-fastapi/app/schemas/profile.py`
- Create: `backend-fastapi/app/schemas/weekly_plan.py`
- Create: `backend-fastapi/app/schemas/badge.py`

**Step 1: base.py — CamelSchema 작성**

설계서 B.3의 `schemas/base.py` 코드를 그대로 작성.

**Step 2: profile.py 작성**

설계서 B.3의 `schemas/profile.py` 코드를 그대로 작성.

**Step 3: weekly_plan.py, badge.py 스키마 작성**

기존 Ktor Backend의 DTO를 참고하여 작성. 기존 API 계약(camelCase JSON)을 유지:

```python
# schemas/weekly_plan.py
from app.schemas.base import CamelSchema

class WeeklyPlanRequest(CamelSchema):
    week_start: str
    day_plans: str  # JSON string

class WeeklyPlanResponse(CamelSchema):
    week_start: str
    day_plans: str
    created_at: str | None = None

class CompletionRequest(CamelSchema):
    week_start: str
    day_index: int
    exercise_index: int
    completed: bool
```

```python
# schemas/badge.py
from app.schemas.base import CamelSchema

class BadgeResponse(CamelSchema):
    badge_key: str
    earned_at: str
```

**Step 4: __init__.py 작성**

빈 파일.

**Step 5: Commit**

```bash
git add backend-fastapi/app/schemas/
git commit -m "feat(backend): Pydantic 스키마 (CamelCase 변환 포함)"
```

---

### Task 5: Repository 레이어

**Files:**
- Create: `backend-fastapi/app/repositories/__init__.py`
- Create: `backend-fastapi/app/repositories/profile_repo.py`
- Create: `backend-fastapi/app/repositories/weekly_plan_repo.py`
- Create: `backend-fastapi/app/repositories/badge_repo.py`

**Step 1: profile_repo.py 작성**

설계서 B.3의 `repositories/profile_repo.py` 코드를 그대로 작성.

**Step 2: weekly_plan_repo.py 작성**

기존 Ktor 라우트의 DB 로직을 참고하여 Repository로 분리:

```python
from sqlalchemy import select, and_
from sqlalchemy.ext.asyncio import AsyncSession
from app.models.weekly_plan import WeeklyPlan
import datetime

class WeeklyPlanRepository:
    def __init__(self, db: AsyncSession):
        self.db = db

    async def get_by_user_and_week(self, user_id: str, week_start: datetime.date) -> WeeklyPlan | None:
        result = await self.db.execute(
            select(WeeklyPlan).where(
                and_(WeeklyPlan.user_id == user_id, WeeklyPlan.week_start == week_start)
            )
        )
        return result.scalar_one_or_none()

    async def upsert(self, user_id: str, week_start: datetime.date, day_plans: str) -> WeeklyPlan:
        plan = await self.get_by_user_and_week(user_id, week_start)
        if plan:
            plan.day_plans = day_plans
        else:
            plan = WeeklyPlan(user_id=user_id, week_start=week_start, day_plans=day_plans)
            self.db.add(plan)
        return plan

    async def get_history(self, user_id: str, page: int, size: int) -> list[WeeklyPlan]:
        result = await self.db.execute(
            select(WeeklyPlan)
            .where(WeeklyPlan.user_id == user_id)
            .order_by(WeeklyPlan.week_start.desc())
            .offset(page * size)
            .limit(size)
        )
        return list(result.scalars().all())

    async def delete_all_by_user(self, user_id: str) -> None:
        from sqlalchemy import delete
        await self.db.execute(delete(WeeklyPlan).where(WeeklyPlan.user_id == user_id))
```

**Step 3: badge_repo.py 작성**

```python
from sqlalchemy import select, delete
from sqlalchemy.ext.asyncio import AsyncSession
from app.models.badge import Badge

VALID_BADGE_KEYS = {"week_1_complete", "week_2_complete", "streak_3weeks"}

class BadgeRepository:
    def __init__(self, db: AsyncSession):
        self.db = db

    async def get_all_by_user(self, user_id: str) -> list[Badge]:
        result = await self.db.execute(
            select(Badge).where(Badge.user_id == user_id)
        )
        return list(result.scalars().all())

    async def award(self, user_id: str, badge_key: str) -> Badge:
        badge = Badge(user_id=user_id, badge_key=badge_key)
        self.db.add(badge)
        return badge

    async def exists(self, user_id: str, badge_key: str) -> bool:
        from sqlalchemy import and_
        result = await self.db.execute(
            select(Badge).where(and_(Badge.user_id == user_id, Badge.badge_key == badge_key))
        )
        return result.scalar_one_or_none() is not None

    async def delete_all_by_user(self, user_id: str) -> None:
        await self.db.execute(delete(Badge).where(Badge.user_id == user_id))
```

**Step 4: Commit**

```bash
git add backend-fastapi/app/repositories/
git commit -m "feat(backend): Repository 레이어 (profile, weekly_plan, badge)"
```

---

### Task 6: Service 레이어

**Files:**
- Create: `backend-fastapi/app/services/__init__.py`
- Create: `backend-fastapi/app/services/profile_service.py`
- Create: `backend-fastapi/app/services/weekly_plan_service.py`
- Create: `backend-fastapi/app/services/badge_service.py`
- Create: `backend-fastapi/app/services/account_service.py`

**Step 1: profile_service.py 작성**

설계서 B.3의 `services/profile_service.py` 코드를 그대로 작성.

**Step 2: weekly_plan_service.py 작성**

```python
from sqlalchemy.ext.asyncio import AsyncSession
from app.repositories.weekly_plan_repo import WeeklyPlanRepository
from app.schemas.weekly_plan import WeeklyPlanRequest, WeeklyPlanResponse, CompletionRequest
from app.exceptions import NotFoundException
import datetime, json

class WeeklyPlanService:
    def __init__(self, db: AsyncSession):
        self.repo = WeeklyPlanRepository(db)

    async def get_plan(self, user_id: str, week_start: str) -> WeeklyPlanResponse:
        date = datetime.date.fromisoformat(week_start)
        plan = await self.repo.get_by_user_and_week(user_id, date)
        if not plan:
            raise NotFoundException("주간 운동 계획이 없습니다")
        return WeeklyPlanResponse(
            week_start=str(plan.week_start),
            day_plans=plan.day_plans,
            created_at=str(plan.created_at) if plan.created_at else None,
        )

    async def upsert_plan(self, user_id: str, req: WeeklyPlanRequest) -> None:
        date = datetime.date.fromisoformat(req.week_start)
        await self.repo.upsert(user_id, date, req.day_plans)

    async def update_completion(self, user_id: str, req: CompletionRequest) -> None:
        date = datetime.date.fromisoformat(req.week_start)
        plan = await self.repo.get_by_user_and_week(user_id, date)
        if not plan:
            raise NotFoundException("주간 운동 계획이 없습니다")
        days = json.loads(plan.day_plans)
        days[req.day_index]["exercises"][req.exercise_index]["completed"] = req.completed
        plan.day_plans = json.dumps(days)

    async def get_history(self, user_id: str, page: int, size: int) -> list[WeeklyPlanResponse]:
        size = min(size, 50)
        plans = await self.repo.get_history(user_id, page, size)
        return [
            WeeklyPlanResponse(
                week_start=str(p.week_start),
                day_plans=p.day_plans,
                created_at=str(p.created_at) if p.created_at else None,
            )
            for p in plans
        ]
```

**Step 3: badge_service.py 작성**

```python
from sqlalchemy.ext.asyncio import AsyncSession
from app.repositories.badge_repo import BadgeRepository, VALID_BADGE_KEYS
from app.schemas.badge import BadgeResponse
from app.exceptions import BadRequestException, ConflictException

class BadgeService:
    def __init__(self, db: AsyncSession):
        self.repo = BadgeRepository(db)

    async def get_badges(self, user_id: str) -> list[BadgeResponse]:
        badges = await self.repo.get_all_by_user(user_id)
        return [BadgeResponse(badge_key=b.badge_key, earned_at=str(b.earned_at)) for b in badges]

    async def award_badge(self, user_id: str, badge_key: str) -> None:
        if badge_key not in VALID_BADGE_KEYS:
            raise BadRequestException(f"유효하지 않은 배지: {badge_key}")
        if await self.repo.exists(user_id, badge_key):
            raise ConflictException(f"이미 획득한 배지: {badge_key}")
        await self.repo.award(user_id, badge_key)
```

**Step 4: account_service.py 작성**

설계서 B.3의 `services/account_service.py` 코드를 그대로 작성 (Auth 먼저 삭제 → DB 삭제 순서).
`from app.exceptions import AppException` import 추가.

**Step 5: Commit**

```bash
git add backend-fastapi/app/services/
git commit -m "feat(backend): Service 레이어 (profile, weekly_plan, badge, account)"
```

---

### Task 7: JWT 인증 + 라우터

**Files:**
- Create: `backend-fastapi/app/dependencies.py`
- Create: `backend-fastapi/app/routers/__init__.py`
- Create: `backend-fastapi/app/routers/health.py`
- Create: `backend-fastapi/app/routers/profile.py`
- Create: `backend-fastapi/app/routers/weekly_plan.py`
- Create: `backend-fastapi/app/routers/badge.py`
- Create: `backend-fastapi/app/routers/account.py`

**Step 1: dependencies.py 작성**

설계서 B.3의 `app/dependencies.py` 코드를 그대로 작성 (PyJWKClient 사용).

**Step 2: health.py 라우터**

```python
from fastapi import APIRouter

router = APIRouter(tags=["health"])

@router.get("/health")
async def health():
    return {"status": "ok"}
```

**Step 3: profile.py 라우터**

설계서 B.3의 `app/routers/profile.py` 코드를 그대로 작성.

**Step 4: weekly_plan.py 라우터**

```python
from fastapi import APIRouter, Depends, Query
from sqlalchemy.ext.asyncio import AsyncSession
from app.dependencies import get_current_user_id
from app.database import get_db
from app.services.weekly_plan_service import WeeklyPlanService
from app.schemas.weekly_plan import WeeklyPlanRequest, WeeklyPlanResponse, CompletionRequest
import datetime

router = APIRouter(tags=["weekly-plan"])

@router.get("/weekly-plan", response_model=WeeklyPlanResponse)
async def get_plan(
    week_start: str = Query(default_factory=lambda: str(datetime.date.today())),
    user_id: str = Depends(get_current_user_id),
    db: AsyncSession = Depends(get_db),
):
    return await WeeklyPlanService(db).get_plan(user_id, week_start)

@router.post("/weekly-plan")
async def create_plan(
    req: WeeklyPlanRequest,
    user_id: str = Depends(get_current_user_id),
    db: AsyncSession = Depends(get_db),
):
    await WeeklyPlanService(db).upsert_plan(user_id, req)
    return {"status": "ok"}

@router.patch("/weekly-plan/complete")
async def complete(
    req: CompletionRequest,
    user_id: str = Depends(get_current_user_id),
    db: AsyncSession = Depends(get_db),
):
    await WeeklyPlanService(db).update_completion(user_id, req)
    return {"status": "ok"}

@router.get("/weekly-plan/history", response_model=list[WeeklyPlanResponse])
async def history(
    page: int = Query(0, ge=0),
    size: int = Query(10, ge=1, le=50),
    user_id: str = Depends(get_current_user_id),
    db: AsyncSession = Depends(get_db),
):
    return await WeeklyPlanService(db).get_history(user_id, page, size)
```

**Step 5: badge.py, account.py 라우터 작성**

```python
# routers/badge.py
from fastapi import APIRouter, Depends
from sqlalchemy.ext.asyncio import AsyncSession
from app.dependencies import get_current_user_id
from app.database import get_db
from app.services.badge_service import BadgeService
from app.schemas.badge import BadgeResponse

router = APIRouter(tags=["badges"])

@router.get("/badges", response_model=list[BadgeResponse])
async def get_badges(user_id: str = Depends(get_current_user_id), db: AsyncSession = Depends(get_db)):
    return await BadgeService(db).get_badges(user_id)

@router.post("/badges/{key}")
async def award_badge(key: str, user_id: str = Depends(get_current_user_id), db: AsyncSession = Depends(get_db)):
    await BadgeService(db).award_badge(user_id, key)
    return {"status": "ok"}
```

```python
# routers/account.py
from fastapi import APIRouter, Depends
from sqlalchemy.ext.asyncio import AsyncSession
from app.dependencies import get_current_user_id
from app.database import get_db
from app.config import get_settings, Settings
from app.services.account_service import AccountService

router = APIRouter(tags=["account"])

@router.delete("/account")
async def delete_account(
    user_id: str = Depends(get_current_user_id),
    db: AsyncSession = Depends(get_db),
    settings: Settings = Depends(get_settings),
):
    await AccountService(db, settings).delete_account(user_id)
    return {"status": "ok"}
```

**Step 6: Commit**

```bash
git add backend-fastapi/app/dependencies.py backend-fastapi/app/routers/
git commit -m "feat(backend): JWT 인증 + 라우터 (health, profile, weekly_plan, badge, account)"
```

---

### Task 8: main.py + Docker 환경

**Files:**
- Create: `backend-fastapi/app/main.py`
- Create: `backend-fastapi/Dockerfile`
- Create: `backend-fastapi/docker-compose.yml`

**Step 1: main.py 작성**

설계서 B.3의 `app/main.py` 코드를 그대로 작성. CORS는 lifespan 내부에서 settings 참조.

**Step 2: Dockerfile 작성**

설계서 B.5의 Dockerfile 코드를 그대로 작성.

**Step 3: docker-compose.yml 작성**

설계서 B.4의 docker-compose.yml 코드를 그대로 작성.

**Step 4: 로컬 실행 확인**

Run: `cd backend-fastapi && docker compose up -d && sleep 5 && curl -sf http://localhost:8080/health`
Expected: `{"status":"ok"}`

**Step 5: Commit**

```bash
git add backend-fastapi/app/main.py backend-fastapi/Dockerfile backend-fastapi/docker-compose.yml
git commit -m "feat(backend): main.py + Docker 로컬 개발 환경"
```

---

### Task 9: Alembic 마이그레이션 설정

**Files:**
- Create: `backend-fastapi/alembic.ini`
- Create: `backend-fastapi/alembic/env.py`
- Create: `backend-fastapi/alembic/versions/` (빈 디렉토리)

**Step 1: alembic init 실행**

Run: `cd backend-fastapi && alembic init alembic`

**Step 2: alembic.ini 수정**

`sqlalchemy.url` 라인을 빈 값으로 설정 (env.py에서 동적 로딩).

**Step 3: env.py 교체**

설계서 B.7의 `alembic/env.py` 코드로 교체.

**Step 4: 초기 마이그레이션 생성**

Run: `cd backend-fastapi && alembic revision --autogenerate -m "initial tables"`
Expected: `alembic/versions/` 에 마이그레이션 파일 생성

**Step 5: Commit**

```bash
git add backend-fastapi/alembic/ backend-fastapi/alembic.ini
git commit -m "feat(backend): Alembic async 마이그레이션 설정"
```

---

## Phase 2: Backend 테스트 (설계서 D.2)

### Task 10: Backend 테스트

**Files:**
- Create: `backend-fastapi/tests/__init__.py`
- Create: `backend-fastapi/tests/conftest.py`
- Create: `backend-fastapi/tests/test_health.py`
- Create: `backend-fastapi/tests/test_profile.py`
- Create: `backend-fastapi/tests/test_weekly_plan.py`
- Create: `backend-fastapi/tests/test_badge.py`
- Create: `backend-fastapi/tests/test_account.py`

**Step 1: conftest.py 작성**

설계서 D.2의 `tests/conftest.py` 코드를 그대로 작성. SQLite in-memory + dependency override.

**Step 2: test_health.py**

```python
import pytest

@pytest.mark.asyncio
async def test_health(client):
    resp = await client.get("/health")
    assert resp.status_code == 200
    assert resp.json() == {"status": "ok"}
```

**Step 3: test_profile.py**

```python
import pytest

@pytest.mark.asyncio
async def test_create_and_get_profile(client):
    resp = await client.put("/profile", json={"heightCm": 175.0, "weightKg": 70.0})
    assert resp.status_code == 200

    resp = await client.get("/profile")
    assert resp.status_code == 200
    data = resp.json()
    assert data["heightCm"] == 175.0
    assert data["weightKg"] == 70.0

@pytest.mark.asyncio
async def test_get_profile_not_found(client):
    resp = await client.get("/profile")
    assert resp.status_code == 404
```

**Step 4: test_weekly_plan.py**

```python
import pytest

@pytest.mark.asyncio
async def test_create_and_get_plan(client):
    plan = {"weekStart": "2026-05-25", "dayPlans": '[{"day":1,"exercises":[]}]'}
    resp = await client.post("/weekly-plan", json=plan)
    assert resp.status_code == 200

    resp = await client.get("/weekly-plan", params={"week_start": "2026-05-25"})
    assert resp.status_code == 200
    assert resp.json()["weekStart"] == "2026-05-25"

@pytest.mark.asyncio
async def test_get_plan_not_found(client):
    resp = await client.get("/weekly-plan", params={"week_start": "2099-01-01"})
    assert resp.status_code == 404

@pytest.mark.asyncio
async def test_history_empty(client):
    resp = await client.get("/weekly-plan/history")
    assert resp.status_code == 200
    assert resp.json() == []
```

**Step 5: test_badge.py**

```python
import pytest

@pytest.mark.asyncio
async def test_award_and_list_badges(client):
    resp = await client.post("/badges/week_1_complete")
    assert resp.status_code == 200

    resp = await client.get("/badges")
    assert resp.status_code == 200
    assert len(resp.json()) == 1
    assert resp.json()[0]["badgeKey"] == "week_1_complete"

@pytest.mark.asyncio
async def test_award_duplicate_badge(client):
    await client.post("/badges/week_1_complete")
    resp = await client.post("/badges/week_1_complete")
    assert resp.status_code == 409

@pytest.mark.asyncio
async def test_award_invalid_badge(client):
    resp = await client.post("/badges/invalid_key")
    assert resp.status_code == 400
```

**Step 6: test_account.py**

```python
import pytest
from unittest.mock import AsyncMock, patch

@pytest.mark.asyncio
async def test_delete_account(client):
    # 프로필 생성
    await client.put("/profile", json={"heightCm": 175.0, "weightKg": 70.0})

    # Supabase API mock
    with patch("app.services.account_service.httpx.AsyncClient") as mock_client:
        mock_resp = AsyncMock()
        mock_resp.status_code = 200
        mock_client.return_value.__aenter__.return_value.delete = AsyncMock(return_value=mock_resp)

        resp = await client.delete("/account")
        assert resp.status_code == 200

    # 프로필 삭제 확인
    resp = await client.get("/profile")
    assert resp.status_code == 404
```

**Step 7: 전체 테스트 실행**

Run: `cd backend-fastapi && pytest tests/ -v`
Expected: 모든 테스트 PASS

**Step 8: Commit**

```bash
git add backend-fastapi/tests/
git commit -m "test(backend): 전체 API 테스트 (health, profile, weekly_plan, badge, account)"
```

---

## Phase 3: CI/CD (설계서 C)

### Task 11: GitHub Actions 워크플로우

**Files:**
- Create: `.github/workflows/backend.yml`

**Step 1: backend.yml 작성**

설계서 C.2의 워크플로우 코드를 그대로 작성. 단, `paths`를 `backend-fastapi/**`로 변경.

**Step 2: Commit**

```bash
git add .github/workflows/backend.yml
git commit -m "ci: Backend CI/CD 워크플로우 (Python + Azure)"
```

---

## Phase 4: Android 에러 핸들링 통일 (설계서 A)

### Task 12: AppError 도메인 타입

**Files:**
- Create: `app/src/main/java/com/gunnys/eundunhealth/domain/model/AppError.kt`

**Step 1: AppError.kt 작성**

설계서 A.1의 `AppError` sealed class + `Throwable.toAppError()` 확장 함수를 그대로 작성.

**Step 2: 빌드 확인**

Run: `./gradlew :app:compileDebugKotlin`
Expected: BUILD SUCCESSFUL

**Step 3: Commit**

```bash
git add app/src/main/java/com/gunnys/eundunhealth/domain/model/AppError.kt
git commit -m "feat(android): AppError 도메인 에러 타입"
```

---

### Task 13: ViewModel 에러 패턴 통일

**Files:**
- Modify: 7개 ViewModel 파일 (설계서 A.2 패턴 적용)

**Step 1: 각 ViewModel에 _error StateFlow + clearError() 추가**

7개 ViewModel을 순회하며:
1. `_error: MutableStateFlow<AppError?>` 추가
2. `error: StateFlow<AppError?>` expose
3. `clearError()` 메서드 추가
4. 기존 에러 핸들링을 `runCatching { ... }.onFailure { _error.value = it.toAppError() }` 패턴으로 통일

대상: `AuthViewModel`, `HomeViewModel`, `OnboardingViewModel`, `ProfileViewModel`, `WorkoutDetailViewModel`, `HistoryViewModel`, `BadgeViewModel`

**Step 2: 빌드 확인**

Run: `./gradlew :app:compileDebugKotlin`
Expected: BUILD SUCCESSFUL

**Step 3: Commit**

```bash
git add app/src/main/java/com/gunnys/eundunhealth/ui/
git commit -m "refactor(android): ViewModel 에러 패턴 통일 (AppError + toAppError)"
```

---

### Task 14: ProGuard 정리

**Files:**
- Modify: `app/proguard-rules.pro`

**Step 1: ProGuard 규칙을 설계서 A.4 내용으로 정리**

기존 내용을 검토하고, 설계서의 최소 필요 규칙으로 정리.

**Step 2: Release 빌드 확인**

Run: `./gradlew clean assembleRelease`
Expected: BUILD SUCCESSFUL

**Step 3: Commit**

```bash
git add app/proguard-rules.pro
git commit -m "chore(android): ProGuard 규칙 정리"
```

---

## Phase 5: 비밀번호 재설정 (설계서 E)

### Task 15: ForgotPasswordScreen + AuthViewModel 확장

**Files:**
- Create: `app/src/main/java/com/gunnys/eundunhealth/ui/auth/ForgotPasswordScreen.kt`
- Modify: `app/src/main/java/com/gunnys/eundunhealth/ui/auth/AuthViewModel.kt`
- Modify: `app/src/main/java/com/gunnys/eundunhealth/ui/auth/LoginScreen.kt`
- Modify: `app/src/main/java/com/gunnys/eundunhealth/ui/navigation/Screen.kt`
- Modify: `app/src/main/java/com/gunnys/eundunhealth/ui/navigation/AppNavigation.kt`

**Step 1: AuthViewModel에 ResetState + resetPassword() 추가**

설계서 E의 `ResetState` sealed class와 `resetPassword()` 코드를 그대로 작성.

**Step 2: ForgotPasswordScreen.kt 작성**

설계서 E의 `ForgotPasswordScreen` 명세에 따라 Compose 화면 구현:
- TextField(이메일) + Button("재설정 링크 보내기")
- 성공: SnackBar → onNavigateBack
- 실패: SnackBar 에러

**Step 3: Screen.kt에 ForgotPassword 추가**

```kotlin
data object ForgotPassword : Screen("forgot_password")
```

**Step 4: LoginScreen.kt에 "비밀번호를 잊으셨나요?" 링크 추가**

**Step 5: AppNavigation.kt에 라우팅 추가**

**Step 6: 빌드 확인**

Run: `./gradlew :app:compileDebugKotlin`
Expected: BUILD SUCCESSFUL

**Step 7: Commit**

```bash
git add app/src/main/java/com/gunnys/eundunhealth/ui/
git commit -m "feat(android): 비밀번호 재설정 화면 (ForgotPasswordScreen)"
```

---

## Phase 6: UX 개선 (설계서 G)

### Task 16: ErrorContent + EmptyContent 컴포넌트

**Files:**
- Create: `app/src/main/java/com/gunnys/eundunhealth/ui/components/ErrorContent.kt`
- Create: `app/src/main/java/com/gunnys/eundunhealth/ui/components/EmptyContent.kt`

**Step 1: ErrorContent.kt 작성**

설계서 G의 `ErrorContent` 명세에 따라 구현:
- 64dp ErrorOutline 아이콘 + `error.userMessage` + "다시 시도" 버튼 (optional onRetry)

**Step 2: EmptyContent.kt 작성**

설계서 G의 `EmptyContent` 명세에 따라 구현:
- 메시지 + 선택적 액션 버튼

**Step 3: HomeScreen, HistoryScreen, BadgeScreen, ProfileScreen에 에러 상태 적용**

각 화면의 에러 상태에서 `ErrorContent(error = viewModel.error, onRetry = { viewModel.refresh() })` 사용.

**Step 4: HomeScreen, HistoryScreen에 PullToRefreshBox 적용**

**Step 5: 빌드 확인**

Run: `./gradlew :app:compileDebugKotlin`
Expected: BUILD SUCCESSFUL

**Step 6: Commit**

```bash
git add app/src/main/java/com/gunnys/eundunhealth/ui/
git commit -m "feat(android): ErrorContent/EmptyContent 컴포넌트 + Pull-to-refresh"
```

---

## Phase 7: 회원 탈퇴 (설계서 F)

### Task 17: Android 회원 탈퇴 UI + API 연동

**Files:**
- Modify: `app/src/main/java/com/gunnys/eundunhealth/data/remote/api/EundunApi.kt`
- Modify: `app/src/main/java/com/gunnys/eundunhealth/data/repository/AuthRepositoryImpl.kt` (또는 UserRepositoryImpl)
- Modify: `app/src/main/java/com/gunnys/eundunhealth/ui/profile/ProfileViewModel.kt`
- Modify: `app/src/main/java/com/gunnys/eundunhealth/ui/profile/ProfileScreen.kt`

**Step 1: EundunApi.kt에 deleteAccount 추가**

```kotlin
@DELETE("account")
suspend fun deleteAccount(): Response<Unit>
```

**Step 2: Repository에 deleteAccount 구현**

Backend API 호출 후 supabaseClient.auth.signOut().

**Step 3: ProfileViewModel에 DeleteState + deleteAccount() 추가**

설계서 F의 `DeleteState` sealed class와 `deleteAccount()` 코드를 구현.

**Step 4: ProfileScreen에 "계정 삭제" 버튼 + AlertDialog 추가**

빨간색 버튼 + 확인 다이얼로그. 성공 시 Login 화면 이동, backStack 클리어.

**Step 5: 빌드 확인**

Run: `./gradlew :app:compileDebugKotlin`
Expected: BUILD SUCCESSFUL

**Step 6: Commit**

```bash
git add app/src/main/java/com/gunnys/eundunhealth/
git commit -m "feat(android): 회원 탈퇴 기능 (Backend API 연동)"
```

---

## Phase 8: 모니터링 (설계서 H)

### Task 18: 모니터링 설정 확인 + Azure 비용 알림

**Files:**
- 변경 없음 (Android Sentry 이미 설정됨, Backend main.py에 이미 포함)

**Step 1: Android Sentry 설정 확인**

`EundunHealthApplication.kt`에서 tracesSampleRate가 DEBUG=1.0/PROD=0.2인지 확인. 변경 불필요.

**Step 2: Backend Sentry 확인**

`backend-fastapi/app/main.py`의 lifespan에서 환경별 traces_sample_rate 분기 확인. 이미 Task 8에서 작성됨.

**Step 3: Azure 비용 관리 명령어 문서화**

README 또는 ops 문서에 기록:
```bash
az acr config retention update --registry eundunhealthacr --days 30 --status enabled
az consumption budget create --amount 70000 --budget-name eundunhealth-budget --category Cost --time-grain Monthly
```

**Step 4: Commit**

```bash
git commit -m "docs: 모니터링 및 Azure 비용 관리 설정"
```

---

## Phase 9: 기존 Ktor Backend 정리

### Task 19: backend → backend-ktor 리네이밍 및 정리

**Step 1: 기존 backend/ 디렉토리를 backend-ktor/로 리네이밍**

```bash
git mv backend backend-ktor
```

**Step 2: backend-fastapi/ → backend/로 리네이밍**

```bash
git mv backend-fastapi backend
```

**Step 3: CLAUDE.md 업데이트**

Backend 섹션을 FastAPI로 변경. 빌드/실행 명령어 업데이트.

**Step 4: Commit**

```bash
git add .
git commit -m "refactor: Backend Ktor→FastAPI 전환 완료, 기존 Ktor 코드 보존 (backend-ktor/)"
```

---

## Phase 10: 마이그레이션 준비 (설계서 O)

### Task 20: Ktor 최종 이미지 보존 + 환경변수 매핑 문서화

**Files:**
- Create: `docs/ops/migration-runbook.md`

**Step 1: Ktor 최종 이미지 태그 보존**

```bash
docker tag eundunhealthacr.azurecr.io/eundunhealth-api:latest eundunhealthacr.azurecr.io/eundunhealth-api:ktor-final
docker push eundunhealthacr.azurecr.io/eundunhealth-api:ktor-final
```

**Step 2: 마이그레이션 런북 작성**

환경변수 매핑표, DB 연결문자열 변환(JDBC→asyncpg), 롤백 절차를 문서화:

```markdown
# Migration Runbook: Ktor → FastAPI

## 환경변수 변환
| Ktor | FastAPI | 변환 |
|------|---------|------|
| AZURE_DB_URL (jdbc:postgresql://...) | DATABASE_URL (postgresql+asyncpg://...) | 프로토콜 변경 |
| AZURE_DB_USER + AZURE_DB_PASSWORD | DATABASE_URL에 내장 | URL 통합 |
| SUPABASE_JWT_SECRET | (삭제) | JWKS 방식 |
| SENTRY_BACKEND_DSN | SENTRY_DSN | 키 이름 변경 |
| ALLOWED_ORIGINS | CORS_ORIGINS | 키 이름 변경 |
| ENV | ENVIRONMENT | 키 이름 변경 |

## Alembic 프로덕션 초기화
alembic stamp head  # 기존 테이블 보존, 버전 기록만

## 롤백
az containerapp update --name eundunhealth-api --resource-group apps \
  --image eundunhealthacr.azurecr.io/eundunhealth-api:ktor-final
```

**Step 3: Commit**

```bash
git add docs/ops/migration-runbook.md
git commit -m "docs: Ktor→FastAPI 마이그레이션 런북"
```

---

### Task 21: Docker 배포 스크립트 전환

**Files:**
- Modify: `C:/programming/docker/eundunhealth-api/Dockerfile`
- Modify: `C:/programming/docker/eundunhealth-api/redeploy.sh`

**Step 1: Dockerfile을 Python 기반으로 교체**

기존 Gradle multi-stage → Python 단일 스테이지:

```dockerfile
FROM python:3.12-slim

RUN groupadd -r appgroup && useradd -r -g appgroup appuser

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY .. .

RUN chown -R appuser:appgroup /app
USER appuser

EXPOSE 8080
HEALTHCHECK --interval=30s --timeout=5s --start-period=15s --retries=3 \
  CMD python -c "import urllib.request; urllib.request.urlopen('http://localhost:8080/health')" || exit 1
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8080"]
```

**Step 2: redeploy.sh 수정**

```bash
#!/bin/bash
set -e
TAG=$(date +%Y%m%d-%H%M%S)
BACKEND_DIR="C:/programming/apps/eundunHealth/backend"

# 1. Copy backend to Docker context
cp -r "$BACKEND_DIR/app" "$BACKEND_DIR/requirements.txt" "$BACKEND_DIR/alembic" "$BACKEND_DIR/alembic.ini" .

# 2. Build & Push
docker build -t "eundunhealthacr.azurecr.io/eundunhealth-api:${TAG}" \
             -t "eundunhealthacr.azurecr.io/eundunhealth-api:latest" .
docker push "eundunhealthacr.azurecr.io/eundunhealth-api:${TAG}"
docker push "eundunhealthacr.azurecr.io/eundunhealth-api:latest"

# 3. Update Container App
az containerapp update --name eundunhealth-api --resource-group apps \
  --image "eundunhealthacr.azurecr.io/eundunhealth-api:${TAG}"

# 4. Cleanup copied files
rm -rf app requirements.txt alembic alembic.ini

echo "Deployed: ${TAG}"
```

**Step 3: Azure 환경변수 일괄 교체**

```bash
az containerapp update --name eundunhealth-api --resource-group apps \
  --set-env-vars \
    "DATABASE_URL=postgresql+asyncpg://gunny:****@healthapp.postgres.database.azure.com:5432/postgres?ssl=require" \
    "SUPABASE_URL=secretref:supabase-url" \
    "SUPABASE_SERVICE_ROLE_KEY=secretref:supabase-service-role-key" \
    "SENTRY_DSN=secretref:sentry-dsn-backend" \
    "ENVIRONMENT=production"
```

**Step 4: Health Check 확인**

Run: `curl -sf https://<FQDN>/health`
Expected: `{"status":"ok"}`

**Step 5: Commit**

```bash
git commit -m "ops: Docker 배포 스크립트 FastAPI 전환"
```

---

## Phase 11: 레거시 정리 (설계서 P)

### Task 22: .gitignore + 미추적 파일 정리

**Files:**
- Modify: `.gitignore`
- Create: `local.properties.example`

**Step 1: .gitignore에 누락 항목 추가**

```gitignore
# Claude Code 로컬 설정
.claude/

# Gradle 자동생성
gradle/gradle-daemon-jvm.properties

# Python Backend
backend/__pycache__/
backend/.venv/
backend/.env
backend/*.egg-info/
```

**Step 2: local.properties.example 생성**

```properties
# Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=eyJ...

# ExerciseDB
EXERCISEDB_API_KEY=your-api-key

# Backend
BACKEND_BASE_URL=http://10.0.2.2:8080/

# Sentry
SENTRY_DSN=https://...

# Release Signing
RELEASE_STORE_PASSWORD=your-store-password
RELEASE_KEY_PASSWORD=your-key-password
RELEASE_KEY_ALIAS=eundunhealth_store_key
SENTRY_AUTH_TOKEN=sntrys_...
```

**Step 3: 미추적 문서 커밋**

```bash
git add docs/PRD.md docs/TRD.md docs/plans/ scripts/
```

**Step 4: Commit**

```bash
git add .gitignore local.properties.example
git commit -m "chore: .gitignore 정리, local.properties.example 추가, 문서/스크립트 커밋"
```

---

### Task 23: 미사용 의존성 제거

**Files:**
- Modify: `app/build.gradle.kts`

**Step 1: kotlinx-serialization-json 의존성 제거**

`app/build.gradle.kts`에서 `implementation(libs.kotlinx.serialization.json)` 라인 삭제.
이 프로젝트는 Gson을 사용하며, kotlinx-serialization은 선언만 있고 실사용 없음.

**Step 2: 빌드 확인**

Run: `./gradlew :app:compileDebugKotlin`
Expected: BUILD SUCCESSFUL

**Step 3: Commit**

```bash
git add app/build.gradle.kts
git commit -m "chore(android): 미사용 kotlinx-serialization-json 의존성 제거"
```

---

## Phase 12: 리팩토링 — 버그 수정 (설계서 Q)

### Task 24: [CRITICAL] Room 캐시 userId 필터링 버그 수정

**Files:**
- Modify: `app/src/main/java/com/gunnys/eundunhealth/data/local/dao/WeeklyPlanDao.kt`
- Modify: `app/src/main/java/com/gunnys/eundunhealth/data/local/EundunDatabase.kt`
- Modify: `app/src/main/java/com/gunnys/eundunhealth/data/repository/WorkoutRepositoryImpl.kt`

**Step 1: WeeklyPlanDao에 userId 파라미터 추가**

```kotlin
@Dao
interface WeeklyPlanDao {
    @Query("SELECT * FROM weekly_plans WHERE userId = :userId AND weekStart = :weekStart ORDER BY cachedAt DESC LIMIT 1")
    suspend fun getPlan(userId: String, weekStart: String): WeeklyPlanEntity?

    @Insert(onConflict = OnConflictStrategy.REPLACE)
    suspend fun insertPlan(plan: WeeklyPlanEntity)

    @Query("DELETE FROM weekly_plans WHERE userId = :userId AND cachedAt < :timestamp")
    suspend fun deleteOldPlans(userId: String, timestamp: Long)
}
```

**Step 2: EundunDatabase version 1→2**

```kotlin
@Database(entities = [WeeklyPlanEntity::class], version = 2, exportSchema = false)
abstract class EundunDatabase : RoomDatabase() {
    abstract fun weeklyPlanDao(): WeeklyPlanDao
}
```

DatabaseModule에서 `fallbackToDestructiveMigration()` 추가 (캐시 테이블이므로 데이터 손실 허용).

**Step 3: WorkoutRepositoryImpl의 dao.getPlan() 호출부에 userId 추가**

현재 `dao.getPlan(weekStart)` → `dao.getPlan(userId, weekStart)`로 변경.
`dao.deleteOldPlans(timestamp)` → `dao.deleteOldPlans(userId, timestamp)`로 변경.

**Step 4: 빌드 + 기존 테스트 확인**

Run: `./gradlew :app:testDebugUnitTest`
Expected: BUILD SUCCESSFUL, 기존 테스트 PASS

**Step 5: Commit**

```bash
git add app/src/main/java/com/gunnys/eundunhealth/data/local/
git add app/src/main/java/com/gunnys/eundunhealth/data/repository/WorkoutRepositoryImpl.kt
git commit -m "fix(android): [CRITICAL] Room 캐시 userId 필터링 누락 버그 수정"
```

---

### Task 25: [HIGH] TokenAuthenticator 타임아웃 + Release 서명 수정

**Files:**
- Modify: `app/src/main/java/com/gunnys/eundunhealth/data/remote/interceptor/TokenAuthenticator.kt`
- Modify: `app/build.gradle.kts`

**Step 1: TokenAuthenticator에 5초 타임아웃 + 토큰 무효화 추가**

```kotlin
import kotlinx.coroutines.withTimeout

override fun authenticate(route: Route?, response: Response): Request? {
    if (response.request.header("X-Retry-Auth") != null) return null

    return try {
        val newToken = runBlocking {
            withTimeout(5_000) {
                supabaseClient.auth.refreshCurrentSession()
                supabaseClient.auth.currentSessionOrNull()?.accessToken
            }
        }
        if (newToken != null) {
            tokenHolder.set(newToken)
            response.request.newBuilder()
                .header("Authorization", "Bearer $newToken")
                .header("X-Retry-Auth", "true")
                .build()
        } else null
    } catch (e: Exception) {
        tokenHolder.set(null)
        null
    }
}
```

**Step 2: build.gradle.kts release buildType에 signingConfig 연결**

```kotlin
buildTypes {
    release {
        signingConfig = signingConfigs.getByName("release")
        isMinifyEnabled = true
        isShrinkResources = true
        proguardFiles(getDefaultProguardFile("proguard-android-optimize.txt"), "proguard-rules.pro")
    }
}
```

**Step 3: 빌드 확인**

Run: `./gradlew :app:compileDebugKotlin`
Expected: BUILD SUCCESSFUL

**Step 4: Commit**

```bash
git add app/src/main/java/com/gunnys/eundunhealth/data/remote/interceptor/TokenAuthenticator.kt
git add app/build.gradle.kts
git commit -m "fix(android): TokenAuthenticator 5초 타임아웃 추가, release signingConfig 연결"
```

---

### Task 26: [MEDIUM] BadgeRepository 일관성 + Sentry 분리

**Files:**
- Modify: `app/src/main/java/com/gunnys/eundunhealth/domain/repository/BadgeRepository.kt`
- Modify: `app/src/main/java/com/gunnys/eundunhealth/data/repository/BadgeRepositoryImpl.kt`
- Modify: `app/src/main/java/com/gunnys/eundunhealth/domain/usecase/CheckAndAwardBadgesUseCase.kt`
- Modify: `app/src/main/java/com/gunnys/eundunhealth/data/repository/WorkoutRepositoryImpl.kt`
- Modify: `app/src/main/java/com/gunnys/eundunhealth/domain/model/AppError.kt`

**Step 1: BadgeRepository 인터페이스 — hasBadge 반환 타입 변경**

```kotlin
suspend fun hasBadge(badgeKey: String): Result<Boolean>
```

**Step 2: BadgeRepositoryImpl — hasBadge를 Result로 래핑 + TTL 캐시 적용**

```kotlin
private var cachedBadges: List<BadgeDto>? = null
private var cacheTimestamp: Long = 0
private val cacheTtl = 60_000L  // 1분

private suspend fun getOrFetchBadges(): List<BadgeDto> {
    val now = System.currentTimeMillis()
    if (cachedBadges != null && now - cacheTimestamp < cacheTtl) {
        return cachedBadges!!
    }
    return api.getBadges().also {
        cachedBadges = it
        cacheTimestamp = now
    }
}

override suspend fun hasBadge(badgeKey: String): Result<Boolean> = runCatching {
    val badges = getOrFetchBadges()
    badges.any { it.badgeKey == badgeKey }
}
```

**Step 3: CheckAndAwardBadgesUseCase 호출부 수정**

`hasBadge()` 반환이 `Result<Boolean>`이므로 `.getOrDefault(false)` 사용.

**Step 4: WorkoutRepositoryImpl에서 Sentry 직접 호출 제거**

`Sentry.captureException(e)` 4곳을 삭제.

**Step 5: AppError.kt에 reportToSentry() 추가**

```kotlin
fun AppError.reportToSentry() {
    if (this is AppError.Unknown) {
        io.sentry.Sentry.captureException(throwable)
    }
}
```

**Step 6: 빌드 + 테스트 확인**

Run: `./gradlew :app:testDebugUnitTest`
Expected: PASS

**Step 7: Commit**

```bash
git add app/src/main/java/com/gunnys/eundunhealth/domain/ app/src/main/java/com/gunnys/eundunhealth/data/repository/
git commit -m "refactor(android): BadgeRepository Result 일관성, Sentry 분리, TTL 캐시"
```

---

## Phase 13: 성능 개선 (설계서 R)

### Task 27: UI 성능 최적화

**Files:**
- Modify: `app/src/main/java/com/gunnys/eundunhealth/ui/history/HistoryScreen.kt`
- Modify: `app/src/main/java/com/gunnys/eundunhealth/ui/onboarding/OnboardingScreen.kt` (ProfileSlider)
- Modify: `app/src/main/java/com/gunnys/eundunhealth/ui/workout/WorkoutDetailScreen.kt`

**Step 1: HistoryScreen — DateTimeFormatter 싱글턴**

파일 상단 또는 companion object에 선언:

```kotlin
private val dateFormatter = DateTimeFormatter.ofPattern("yyyy년 M월 d일")
```

기존 각 카드에서 `DateTimeFormatter.ofPattern(...)` 호출하는 부분을 `dateFormatter`로 교체.

**Step 2: ProfileSlider — format 결과 remember**

```kotlin
val formattedValue = remember(value) { formatPattern.format(value) }
```

**Step 3: WorkoutDetailScreen — Coil 이미지 사이즈 제약**

```kotlin
AsyncImage(
    model = ImageRequest.Builder(context)
        .data(exercise.gifUrl)
        .size(512)
        .crossfade(true)
        .build(),
    ...
)
```

**Step 4: 빌드 확인**

Run: `./gradlew :app:compileDebugKotlin`
Expected: BUILD SUCCESSFUL

**Step 5: Commit**

```bash
git add app/src/main/java/com/gunnys/eundunhealth/ui/
git commit -m "perf(android): DateTimeFormatter 싱글턴, ProfileSlider remember, Coil 사이즈 제약"
```

---

## 실행 요약

| Phase | Tasks | 내용 |
|-------|-------|------|
| 1 | 1-9 | Backend FastAPI 전체 구현 (~20개 파일) |
| 2 | 10 | Backend 테스트 (5개 파일, ~15개 테스트) |
| 3 | 11 | CI/CD 워크플로우 |
| 4 | 12-14 | Android 에러 핸들링 통일 |
| 5 | 15 | 비밀번호 재설정 |
| 6 | 16 | UX 개선 (ErrorContent/EmptyContent/Pull-to-refresh) |
| 7 | 17 | 회원 탈퇴 |
| 8 | 18 | 모니터링 확인 |
| 9 | 19 | Backend 디렉토리 정리 |
| 10 | 20-21 | 마이그레이션 준비 (Ktor 보존, Docker 전환, 환경변수) |
| 11 | 22-23 | 레거시 정리 (.gitignore, 미사용 의존성, 문서 커밋) |
| 12 | 24-26 | 리팩토링 — 버그 수정 (Room 캐시, TokenAuthenticator, Badge, Sentry) |
| 13 | 27 | 성능 개선 (DateTimeFormatter, remember, Coil)
